<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/cancel.css">
</head>
<body>
    <div class="cancel">
        <div class="header">
            <h2>Cancel Slot Request</h2>
        </div>
        <div class="main">
            <div class="user">
            <h4><%=username %></h4>
        </div>
        <div class="table">
            <form action="/cancel" method="post">
                <table class="table">
                    <tr>
                        <th class="table-dark">Select</th>
                        <th class="table-dark">Name</th>
                        <th class="table-dark">Gender</th>
                        <th class="table-dark">Registration Date</th>
                        <th class="table-dark">Exipry Date</th>
                    </tr>
                <% for (const i in cancelmalelist) { %>
                    
                        <tr>
                            <td><input type="checkbox" name="name" value="<%= cancelmalelist[i]._id%>"></td>
                            <td><%= cancelmalelist[i].name %></td>
                            <td><%= cancelmalelist[i].gender %></td>
                            <td><%= cancelmalelist[i].date %></td>
                            <td><%= cancelmalelist[i].expiry%></td>
                        </tr>
                   
                <%}%>
                
                <% for (const i in cancelfemalelist) { %>
                    <tr>
                        <td><input type="checkbox" name="name" value="<%= cancelfemalelist[i]._id%>"></td>
                        <td><%= cancelfemalelist[i].name %></td>
                        <td><%= cancelfemalelist[i].gender %></td>
                        <td><%= cancelfemalelist[i].date %></td>
                        <td><%= cancelfemalelist[i].expiry%></td>
                    </tr>
                <%}%>
                
                
            </div>
        </table>
        <Button type="submit">Confirm</Button>
            </form>


        </div>
    </div>
</body>
</html>